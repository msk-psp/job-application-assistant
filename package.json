{
  "name": "job-application-assistant",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check content.js && node --check options.js && node --check popup.js && node --check pdf-import.js && node --check test-form.js && node --test test/*.test.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
